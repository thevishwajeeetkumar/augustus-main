# --- API server stack ---
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
starlette>=0.27.0

# --- Configuration ---
python-dotenv>=1.0.1

# --- Database (SQLAlchemy + Postgres driver + migrations) ---
SQLAlchemy>=2.0.29
alembic>=1.13.2
psycopg2-binary>=2.9.9

# --- Authentication & request parsing ---
bcrypt>=4.1.2
PyJWT>=2.8.0
python-multipart>=0.0.9

# --- LangChain + integrations (compatible versions) ---
# Pin to 0.2.x line to avoid conflicts with langchain-classic/langgraph-prebuilt (which require 1.0.x)
# All packages pinned to <1.0.0 to ensure compatibility
langchain>=0.2.16,<1.0.0
langchain-core>=0.2.24,<1.0.0
langchain-openai>=0.1.16,<1.0.0
langchain-text-splitters>=0.2.1,<1.0.0
langchain-community>=0.2.5,<1.0.0
langchain-experimental>=0.0.63,<1.0.0
langchain-pinecone>=0.2.3,<1.0.0
langchainhub>=0.1.16,<1.0.0
openai>=1.40.0,<2.0.0

# --- Vector database client ---
pinecone-client>=3.0.0

# --- Retrieval helpers ---
youtube-transcript-api>=1.2.3
langdetect>=1.0.9

# --- HTTP utilities ---
requests>=2.32.0

# --- Data validation ---
pydantic>=2.7.0

# --- Additional dependencies (transitive) ---
typing-extensions>=4.5.0
anyio>=4.0.0
sniffio>=1.3.0
idna>=3.4
h11>=0.14
